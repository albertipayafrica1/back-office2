import colors from "../../../styles/colors";

export const radioExternalStyles = {
  radioColor: colors.blue,
};

export const checkboxExternalStyles = {
  checkboxColor: colors.blue,
};

export const checkbox1ExternalStyles = {
  checkboxColor: colors.orange,
  borderColor: colors.orange,
};
